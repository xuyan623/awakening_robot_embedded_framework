# Linux SYNC Port Skeleton

This directory is a Phase 0 skeleton for future Linux sync acceleration backends.

Current status:
- Not active because Linux OS is not in `awlf/platform/osal/index.lua`
- No accelerated `.c` implementation is provided
- Only structure reservation for later phases
